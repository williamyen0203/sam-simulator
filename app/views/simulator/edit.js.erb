// generic solution
// var i = 1;
// <% Simulator.all.each do |food| %>
//   var selector = ".number:nth-child(" + i + ")";
//   $(selector).html(<%= food.amount %>);
//   i++;
// <% end %>

$("#oreo-counter").html(<%= Simulator.find_by_food("oreo").amount %>);
$("#sushi-counter").html(<%= Simulator.find_by_food("sushi").amount %>);
$("#shumai-counter").html(<%= Simulator.find_by_food("shumai").amount %>);
